# Agent instructions entry

The authoritative repository instructions are in [AGENTS.md](AGENTS.md).

`cash/` is the local quarantine for obsolete experiment data and reports. It is
not an active result cache. Do not read, search, enumerate, execute or restore
its contents unless the user explicitly asks. Git ignores everything inside it,
including MATLAB and Markdown files; do not force-add archived content.

Use the archive manifest in `reports/bellhop_100m_rebaseline_plan.md` for the
scope of the completed archive operation, without opening `cash/`.

# 旧 500 m 粗糙海面 Bellhop 报告已退役

2026-08-27：原报告及对应数据、图片、临时试验已按用户要求移入根目录
`cash/` 专用隔离区。这里仅保留迁移提示，不再提供旧结果作为当前环境的结论。

原设置中的海底位于 500 m，不能代表用户现在明确的 **100 m 水深、发射机
离底 0 m** 场景。旧代码保留供后续实现复用，不得按旧默认参数继续运行。

当前需求、海底测试参数、边界源限制、执行计划和归档清单见：
[100 m Bellhop 重建计划](bellhop_100m_rebaseline_plan.md)。

新实验尚未执行。后续不要读取或自动恢复 `cash/`；只有用户显式要求归档操作
时才可访问。既有 PE–AS、Gaussian 窗口和镜像展开验证不在此次退役范围。
